# Tuples
# Similar to lists, has key differences
# Are immutable (can't be changed or modified)

# Example - storing coordinates
coordinates = (4, 5)   # Note () instead of []
print(coordinates[0])  # Access like a list

coordinates_list = [(4, 5), (5, 6)] # A list of tuples